fastapi
uvicorn
python-multipart
httpx
numpy
pydantic>=2
# Pin the ML stack. jina-embeddings-v5's remote code (custom_st.py) is written for transformers
# 4.x; transformers 5.x removed the `config_class` attribute its custom model relies on. Keep
# pinned so a rebuild can't silently bump a major and break loading. jina-reranker-v3 loads via
# AutoModel(trust_remote_code=True) under the same transformers.
transformers==4.57.6
sentence-transformers==5.5.1
peft==0.19.1
einops
# ONNX CPU embedding backend (R06a, #18), opt-in via EMBED_ONNX=1. optimum 2.x split the
# onnxruntime backend into the separate optimum-onnx package; sentence-transformers' onnx loader
# needs optimum.onnxruntime.ORTModelForFeatureExtraction, which lives there. Pin both.
onnxruntime==1.27.0
optimum==2.1.0
optimum-onnx==0.1.0
# torch is provided by the base image / installed separately; do not pin here.
